# compiled output
/dist
/node_modules

# Vite's local dependency pre-bundle cache — regenerated automatically,
# shouldn't be tracked (accidentally committed once via `git add -A` while
# the cache happened to exist on disk; see apps/client/docs/DESIGN_TOKENS.md)
.vite

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# OS
.DS_Store

# Editors
.idea
.vscode/*
!.vscode/extensions.json

# Tests
/coverage

# Environment
.env
.env.local

*.tsbuildinfo
