# Phase 29 — Principal Paydowns (Client)

## Goal

UI for registering a principal-only paydown against a loan, and reflecting its effect on the loan's remaining schedule/balance. See `docs/phases/PHASE_29_PRINCIPAL_PAYDOWN.md`.

## Status

**Blocked on backend design confirmation.** This is new scope with several unresolved questions (whether the remaining installment schedule regenerates, how it interacts with interest-first allocation, effect on cupo) — see the "Open questions" section of the backend doc. Do not build any UI for this until those are confirmed and the backend doc's "Scope" section is filled in; a UI built against a guessed shape would need to be redone.

## Scope

Deliberately left unwritten pending `docs/phases/PHASE_29_PRINCIPAL_PAYDOWN.md`'s open questions.

## Related documents

- `docs/phases/PHASE_29_PRINCIPAL_PAYDOWN.md` — backend design this phase is blocked on
